# build output (rebuild with .\build.ps1)
ClaudePet.exe
*.tmp

# sprite generator leftovers; atlas.png / atlas.txt / icon.ico stay tracked so build.ps1 works without Python
art/__pycache__/
art/build/preview_*.png
